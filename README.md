### Wayback Recon Script
— A bash script that automates passive URL discovery for a target domain using gau, waybackurls, and waymore. Automatically checks for and installs missing dependencies, then merges and deduplicates all results into a single output file. 
